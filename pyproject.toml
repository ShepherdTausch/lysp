[project]
name = "lysp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "humanize>=4.15.0",
    "termcolor>=3.3.0",
]

[project.scripts]
lysp = "src.main:main"
